rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All resort data: orders, menu config, etc.
    match /resorts/{resortId}/{document=**} {
      allow read, write: if true;
    }
  }
}
